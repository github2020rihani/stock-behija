{% extends 'base.html.twig' %}

{% block title %}Lcommande index{% endblock %}

{% block body %}
    <h1>Lcommande index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numc</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for lcommande in lcommandes %}
            <tr>
                <td>{{ lcommande.id }}</td>
                <td>{{ lcommande.Numc }}</td>
                <td>
                    <a href="{{ path('lcommande_show', {'id': lcommande.id}) }}">show</a>
                    <a href="{{ path('lcommande_edit', {'id': lcommande.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('lcommande_new') }}">Create new</a>
{% endblock %}
