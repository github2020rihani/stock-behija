<div class="row">
    <div class="col-sm-12">
        <div class="panel panel-default">
            <div class="panel-control">
                <a class="btn btn-default add-tooltip" data-target="#demo-panel-collapse-default"
                   data-toggle="collapse"
                   data-original-title="Afficher/Cacher"><i class="fa fa-chevron-down"></i></a>
            </div>
            <h3 class="panel-title">Chercher</h3>
            <div id="demo-panel-collapse-default" class="collapse">
                {{ form_start(search_form,{'method': 'get', 'action': path('admin_categories_home'),'attr': {'class': 'with-loading', 'id': 'search-form'}}) }}
                <div class="panel-body panel-search" style="margin-top: -20px;">
                    <div class="col-lg-3">
                        {{ form_row(search_form.name) }}
                    </div>

                </div>
                <div class="row text-center panel-search-footer">
                    <button type="submit" class="btn btn-primary">
                        <i class="fa fa-search"></i> Chercher
                    </button>
                    &nbsp;&nbsp;
                    <button type="reset" class="btn btn-warning">
                        <i class="fa fa-refresh"></i> Réinitialiser
                    </button>
                </div>

                {{ form_end(search_form) }}
            </div>

        </div>
    </div>
</div>