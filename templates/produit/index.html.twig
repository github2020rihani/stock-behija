{% extends 'base.html.twig' %}

{% block title %}Produit index{% endblock %}

{% block body %}

<div class="content-wrapper" style="min-height: 1602px;" xmlns="http://www.w3.org/1999/html">

    <section class="content-header">
        <div class="container-fluid">
            <div class="row mb-2">
                <div class="col-sm-6">
                    <h1>Produit index</h1>
                </div>
            </div>
            <div class="form-group row">

                <div class="col-sm-2" id >
                  {{ form_start(form) }}
                  {{form_row(form)}}

                  <button type="submit" class="btn btn-primary">Search</button>
                   {{form_end(form)}}

                </div>


     <table class="table">
        <thead>
            <tr>

                <th>Code Produit</th>
                <th>Designation</th>
                <th>Quantite</th>
                <th>Image</th>
                <th>Prix</th>
                <th>Tva</th>
                <th>Unite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.code }}</td>
                <td>{{ produit.designation }}</td>
                <td>{{ produit.quantite }}</td>
                <td>{{ produit.imageName }}</td>
                <td>{{ produit.prixHt }}</td>
                <td>{{ produit.tva }}</td>
                <td>{{ produit.unite }}</td>



{#   <!--<td>{{ produit.createdAt ? produit.createdAt|date('Y-m-d H:i:s') : '' }}</td>-->#}{#

                <td>
                    <a href="{{ path('produit_show', {'id': produit.id}) }}">show</a>
                    <a href="{{ path('produit_edit', {'id': produit.id}) }}">edit</a>
                    <a href=" {{ path('produit_delete', { 'id': produit.id }) }}">delete</a>
                </td>
            </tr>

        </tbody>#}
        {% endfor %}
    </table>

            </div>
        </div></section></div>

{% endblock %}